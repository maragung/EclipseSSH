# CODEOWNERS. The single required reviewer for every change to the
# project. GitHub auto-assigns the listed owners on any PR that
# touches the patterns below. The default branch protection rule
# (see docs/branch-protection.md) requires this approval before
# merging.
#
# Adding a new owner: append their GitHub handle to the relevant
# lines, separated by spaces. The last owner on a line is the
# fallback when a previous owner is unavailable.
* @maragung
/.github/ @maragung
/gradle/ @maragung
/app/build.gradle.kts @maragung
/app/src/main/ @maragung
/app/src/test/ @maragung
/app/src/androidTest/ @maragung
