<?xml version="1.0" encoding="utf-8"?>
<!-- A manifest file is optional for AGP library modules; this one exists only
     so lint has a stable main manifest to analyze instead of a generated
     placeholder. The library declares no permissions and no components - it is
     a JNI wrapper plus packaged native artifacts, nothing else. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" />
